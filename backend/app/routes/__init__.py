"""API route blueprints for Exam OS (CBT platform).

Blueprints are registered by ``app.create_app`` with stable ``/api/*`` prefixes.
This package intentionally exports no symbols — import blueprints from their
modules (``app.routes.auth.auth_bp``, etc.).
"""
