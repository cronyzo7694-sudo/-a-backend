"""Shared utilities for Exam OS API routes.

Submodules (import callables from these modules — names are stable)::

    app.utils.decorators   roles_required, get_current_user
    app.utils.validators   require_fields, is_valid_email, is_strong_password,
                           parse_pagination, OPTION_KEYS
"""
