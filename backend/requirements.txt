# Exam OS — Python dependencies (pin exact versions for reproducible CBT deploys)
# Install:  pip install -r requirements.txt
# Postgres: pip install "psycopg[binary]>=3.2.0"

# --- Web framework ---
Flask==3.0.3
Werkzeug==3.0.6
Jinja2==3.1.4
itsdangerous==2.2.0
click==8.1.7
blinker==1.8.2

# --- API / auth ---
Flask-CORS==4.0.1
Flask-JWT-Extended==4.6.0
PyJWT==2.9.0
email-validator==2.2.0

# --- Database ---
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
Flask-Migrate==4.0.7
Alembic==1.14.0
# greenlet is pulled by SQLAlchemy; pin for binary wheel stability
greenlet==3.1.1

# --- Config / ops ---
python-dotenv==1.0.1
python-dateutil==2.9.0.post0
gunicorn==23.0.0
packaging==24.1

# --- Optional serialization helpers (available for future DTO layer) ---
marshmallow==3.23.1

# --- Optional Neon / PostgreSQL (uncomment or install separately) ---
# psycopg[binary]>=3.2.0
